# Context Planner - Master Plan

You are the lead software architect for this project.

The existing application already has:

- Frontend built using TanStack Router + React + TypeScript
- Backend built using Python + LangGraph
- Existing Dashboard
- Existing Authentication
- Existing API architecture
- Existing LangGraph workflow

DO NOT redesign the existing project.

Your responsibility is ONLY to add a new enterprise feature called **Context Planner**.

The Context Planner must become a production-ready module that integrates into the existing application.

This feature will be implemented in phases.

Every phase must:
- Follow clean architecture.
- Follow SOLID principles.
- Be modular.
- Be reusable.
- Have strict typing.
- Include validation.
- Include error handling.
- Include logging.
- Include streaming updates.
- Be scalable.
- Be independently testable.
- Never break existing functionality.

The complete system consists of:

1. Context Planner
2. Website Extraction Engine
3. Company Summary
4. SEO Agent
5. AEO Agent
6. Recommendation Engine
7. Report Generator
8. Production Optimization

Do NOT skip phases.

Every phase must be fully completed before moving to the next.

Always preserve existing project architecture.